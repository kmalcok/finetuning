# Core stack for QLoRA / PEFT fine-tuning
# These are minimum versions known to work together.
# On Google Colab most of these are pre-installed; the notebook installs the rest.

torch>=2.3.0
transformers>=4.44.0
datasets>=2.20.0
accelerate>=0.33.0
peft>=0.12.0
bitsandbytes>=0.43.0
trl>=0.9.0

# Nice to have for logging / inspection
scipy>=1.11.0
sentencepiece>=0.2.0
